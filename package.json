{
  "name": "cluso-impostor-web",
  "version": "1.0.0",
  "description": "Cluso Impostor Game Landing Page",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:i18n": "node build-i18n.js",
    "build:all": "npm run build:i18n && vite build",
    "preview": "vite preview",
    "deploy": "npm run build:all && firebase deploy --only hosting"
  },
  "devDependencies": {
    "terser": "^5.24.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "firebase": "^12.4.0"
  }
}
